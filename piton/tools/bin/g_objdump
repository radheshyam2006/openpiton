#! /bin/sh
# Modernized g_objdump wrapper for RISC-V
args=""
for arg in "$@"; do
    if [ "$arg" != "-m" ] && [ "$arg" != "sparc:v9B" ]; then
        args="$args $arg"
    fi
done
exec riscv64-unknown-elf-objdump $args
